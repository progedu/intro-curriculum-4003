extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
    p Hello, <img height="20" width="20" src="#{user._json.avatar_url}"> #{user.username}
    a(href="/logout") Logout
  else
    a(href="/login") Login
